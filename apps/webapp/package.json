{
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.5.4"
  },
  "scripts": {
    "build": "esbuild frontend/webapp.ts --bundle --minify --sourcemap --outfile=static/webapp.js",
    "watch": "esbuild frontend/webapp.ts --bundle --sourcemap --outfile=static/webapp.js --watch"
  }
}
